import {SecurityDirective} from "./security.directive";

describe('SecurityDirective', () => {
  it('should create an instance', () => {
    // const directive = new SecurityDirective();
    // expect(directive).toBeTruthy();
  });
});
